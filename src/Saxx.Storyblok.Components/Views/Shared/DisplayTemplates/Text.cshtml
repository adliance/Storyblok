@using Markdig
@model Saxx.Storyblok.Components.Text

@{
    var pipeline = new MarkdownPipelineBuilder().UseAdvancedExtensions().Build();
    var content = Markdown.ToHtml(Model.Content, pipeline);
}

@Html.Raw(content)